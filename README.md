Used GitHub Copilot Chat (/tests) to generate the initial unit
test skeleton for calculate_flight_time(). Rewrote 1 of the 5 tests
-- the original didn't check for a typical payload. Verified all tests
pass with pytest.
